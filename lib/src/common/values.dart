import 'package:flutter/rendering.dart';

const appConstraints = BoxConstraints(maxWidth: 1366.0);
const appPadding = EdgeInsets.symmetric(horizontal: 40.0);
const appPaddingSmallScreen = EdgeInsets.symmetric(horizontal: 16.0);
